{
  "name": "woobsbox",
  "version": "1.0.0",
  "description": "Collection of Typescript utility modules to ease development",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "publish": "npm run build && cd dist && npm publish && cd .."
  },
  "keywords": [
    "typescript",
    "utility"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@2toad/profanity": "^2.2.0"
  }
}
